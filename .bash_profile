#!/bin/bash
#
#  _______
#  \_   _|
#    |_|horbits 
#
# My bash config

# Load .bashrc, which loads: ~/.{bash_prompt,aliases,exports,functions}
if [[ -r "${HOME}/.bashrc" ]]; then
	source "${HOME}/.bashrc"
fi
